<div class="box">
  <article class="media">
    <figure class="media-left">
      <p class="image is-48x48 user-thumb">
        <%= image_tag user_avatar(project_comment.user, 50), class: "avatar-image"%>
      </p>
    </figure>
    <div class="media-content">
      <div class="content">
        <p>
          <strong><%= link_to project_comment.user.name, user_path(project_comment.user) %></strong>
          <div><%= project_comment.response %></div>
        </p>
      </div>
    </div>
    <% if (user_signed_in? && (current_user.id == project_comment.user_id)) || current_user.is_admin?%>
       <%= link_to 'Delete', [project_comment.project, project_comment],
                  method: :delete, class: "button is-danger", data: { confirm: 'Are you sure?' } %>
    <% end %>
  </article>
</div>
