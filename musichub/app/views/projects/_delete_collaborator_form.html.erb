<%= form_for(person, url: project_delete_collaborator_path(@project), method: :post) do |f|%>
  <%= f.hidden_field :id %>
  <%= f.submit "Remove", class: "button is-small is-danger inline" %>
<% end %>
